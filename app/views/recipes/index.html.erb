<h1>Coursera Course - <%= link_to 'Ruby on Rails: An Introduction', 'https://www.coursera.org/learn/ruby-on-rails-intro' %></h1>

<h2>Searching for - <%= @search_term %></h2>

<p>Created by Rundong Lyu</p>

<% if @recipes.nil? %>

<p>No recipes found...</p>

<% else %>

<table border="1">
    <th>Image</th>
    <th>Title</th>
    <th>Social Rank</th>
    <% @recipes.each do |recipe| %>
      <tr class=<%= cycle('even', 'odd') %>>
        <td><%= link_to image_tag(recipe['image_url']), recipe['source_url'] %></td>
        <td><%= link_to sanitize(recipe['title']), recipe['source_url'] %></td>
        <td><%= recipe['social_rank'] %></td>
      </tr>
    <% end %>
</table>

<% end %>